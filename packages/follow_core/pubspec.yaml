name: follow_core
description: Platform-independent social follow analysis core.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.6.0 <4.0.0"

dependencies:
  archive: ^4.2.0
  html: ^0.15.7

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.8
